# https://golangci-lint.run/usage/linters/
linters:
  enable-all: true
  disable:
    - contextcheck
    - exhaustruct
    - exhaustivestruct
    - golint
    - interfacer
    - ireturn # Too many false negatives.
    - lll
    - maligned
    - nlreturn # ignore "return with no blank line before"
    - nonamedreturns # unnecessary
    - scopelint
    - varnamelen
    - wsl # ignore "declarations should never be cuddled"

linters-settings:
  varnamelen:
    ignore-decls:
      - l *Logger
      - l *rec.Logger
      - rw http.ResponseWriter
      - w http.ResponseWriter
      - r *http.Request
  wrapcheck:
    ignoreSigs:
      - func fmt.Errorf(format string, a ...any) error
      - func github.com/cockroachdb/errors.Errorf(format string, args ...interface{}) error
      - func (*github.com/rec-logger/rec.go.errorReturner).Err() error

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - containedctx
        - cyclop
        - dupl
        - funlen
        - gochecknoglobals
        - gocognit
        - goconst
        - goerr113
        - gomnd
        - maintidx
        - noctx
        - varnamelen
